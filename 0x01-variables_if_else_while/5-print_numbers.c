#include <stdio.h>
/**
 * main - a fun to print numbers of single digits
 * Return: Always (Success)
 */
int main(void)
{
int i = 0;
for (i = 0 ; i < 10 ; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
