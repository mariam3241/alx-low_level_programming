#include "main.h"

/**
 * _islower - a fun that checks for lowercase character
 * @c: parameter
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
