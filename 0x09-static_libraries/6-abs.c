#include "main.h"

/**
 * _abs -a fun that computes the absolute value of an integer
 * @n: parameter
 * Return: an integer value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

