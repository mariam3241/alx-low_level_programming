#include "main.h"

/**
 * is_palindrome - a fun that checks if a given string is palindrome
 * @s: parameter
 * Return: 1 if a string is palindrome and 0 if not
 */

int is_palindrome(char *s)
{
if (*s == 0)
{
return (1);
}
}
