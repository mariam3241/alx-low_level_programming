#include "main.h"

/**
 * _sqrt_recursion - a fun that returns the square root
 * @n: parameter
 * Return: int value
 */

int _sqrt_recursion(int n)
{
	int sqr;

	if (n < 0)
	{
		return (-1);
	}
	els
	{
		sqr = _sqrt_recursion(n, 0);
		return (sqr);
	}
}
